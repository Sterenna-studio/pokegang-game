name: Legacy OVH Deploy - disabled

# This legacy workflow is intentionally disabled.
# The active deployment workflow is .github/workflows/deploy-ovh.yml
# It deploys the static site to OVH with SSH + rsync.

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Legacy workflow disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled legacy deploy
        run: |
          echo "This legacy OVH git-pull workflow is disabled."
          echo "Use .github/workflows/deploy-ovh.yml instead."
